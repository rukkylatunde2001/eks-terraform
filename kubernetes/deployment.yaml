apiVersion: apps/v1
kind: Deployment
metadata:
  name: eks-app
  labels:
    app: eks-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: eks-app
  template:
    metadata:
      labels:
        app: eks-app
    spec:
      containers:
        - name: eks-app
          image: IMAGE_PLACEHOLDER
          ports:
            - containerPort: 3000